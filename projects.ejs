<% if (items && items.length > 0) { %>
::: {.project-cards}
<% for (const item of items) { %>

::: {.card .border-0}
:::: {.card-body}
::::: {.card-title}
### <%- item.title %>
<% if (item.subtitle) { %>
:::::: {.card-subtitle .text-muted}
##### <%- item.subtitle %>
::::::
<% } %>
:::::

::::: {.card-text}
<%- item.description %>
:::::


<% if (item.links && item.links.length > 0) { %>
<% for (const link of item.links) { %>[<%- link.title %>](<%- link.href %>){.card-link}<% } %>
<% } %>

::::
:::

<% } %>
:::
<% } %>